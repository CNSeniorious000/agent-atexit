// Default MCP discovery and Claude result reminders share one policy; Codex emits no result reminder.
export const cleanupInstruction = "Register scoped fallback cleanup for temporary processes and CLI sessions kept live across tool calls. Cover all newly acquired resources as soon as their real cleanup targets are known. Avoid spending a model turn only on registry bookkeeping when independent task work is ready: use parallel calls or one orchestration invocation. Registration can accompany resource use or inspection. After cleanup succeeds, cancel its fallback alongside work on other resources, including their cleanup. Sequence dependencies within an invocation when possible. Never race cancellation with its own cleanup, delay registration, or invent work to fill a batch.";

// Select the guidance validated in Codex without changing other hosts' defaults.
export const codexCleanupInstruction = "Include eligible atexit bookkeeping in each batch of ready task work. Register as soon as cleanup can target an acquired resource, without waiting for unrelated results. Run independent calls together; sequence dependencies within one orchestration invocation when possible. Cancel only after a release check confirms that the registered target itself is gone. The check must propagate unexpected errors, not convert every exception or nonzero exit into successful release. Combine eligible cancellation with the next independent work. Standalone bookkeeping is fine when no independent work remains. Never invent work or delay registration to batch.";

// Hermes honors successful cleanup contracts without delaying cancellation for redundant proof.
export const hermesCleanupInstruction = "Include eligible atexit bookkeeping in the next batch of ready work. Register every newly acquired resource in the first response after its cleanup target is known. A cleanup result that guarantees release already proves the target is gone; otherwise verify it, preserving diagnostics and distinguishing absence from inspection errors. Cancel then, without waiting for other resources or repeating that proof. Run independent actions together and sequence dependencies within one orchestration when available. Standalone bookkeeping is fine when no independent work remains; never invent work.";
