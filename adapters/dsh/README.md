# @agent-atexit/dsh

DeepSeek Harness adapter for [agent-atexit](https://github.com/CNSeniorious000/agent-atexit). It exposes native register, list, and cancel tools and drains the deferred command stack from the owning Agent scope disposer.

See the repository README for installation, security, and lifecycle limitations.
