{
  "mcpServers": {
    "atexit": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/dist/mcp.mjs", "--claude"],
      "env": {
        "AGENT_ATEXIT_STATE_DIR": "${CLAUDE_PLUGIN_DATA}/atexit"
      }
    }
  }
}
